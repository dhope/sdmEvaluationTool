version: 0.1

roles:

  viewer:
    name: Viewer
    materials_read: Yes
    materials_create_new: No
    materials_edit_own: No
    materials_edit_all: No
    materials_delete: No
    evaluations_read: Yes
    evaluations_create_new: No
    evaluations_edit_own: No
    evaluations_edit_all: No
    evaluations_delete: No
    discussions_read: No
    discussions_write: No

  commenter:
    name: Commenter
    materials_read: Yes
    materials_create_new: No
    materials_edit_own: No
    materials_edit_all: No
    materials_delete: No
    evaluations_read: Yes
    evaluations_create_new: No
    evaluations_edit_own: No
    evaluations_edit_all: No
    evaluations_delete: No
    discussions_read: Yes
    discussions_write: Yes

  evaluator:
    name: Evaluator
    materials_read: Yes
    materials_create_new: No
    materials_edit_own: No
    materials_edit_all: No
    materials_delete: No
    evaluations_read: Yes
    evaluations_create_new: Yes
    evaluations_edit_own: Yes
    evaluations_edit_all: No
    evaluations_delete: No
    discussions_read: Yes
    discussions_write: Yes

  modeler:
    name: Modeler
    materials_read: Yes
    materials_create_new: Yes
    materials_edit_own: Yes
    materials_edit_all: No
    materials_delete: No
    evaluations_read: Yes
    evaluations_create_new: No
    evaluations_edit_own: No
    evaluations_edit_all: No
    evaluations_delete: No
    discussions_read: Yes
    discussions_write: Yes

  admin:
    name: Admin
    materials_read: Yes
    materials_create_new: Yes
    materials_edit_own: Yes
    materials_edit_all: Yes
    materials_delete: Yes
    evaluations_read: Yes
    evaluations_create_new: Yes
    evaluations_edit_own: Yes
    evaluations_edit_all: Yes
    evaluations_delete: Yes
    discussions_read: Yes
    discussions_write: Yes

tables:

  users:
    name: Users
    description: >
      This table contains user names and admin roles, and IDs be used as foreign keys in 
      other tables to track change history. This table needs to be updated 
      regularly, requires **admin** access and writing directly to the database. 
      See the list of user roles for specific permissions.
      In the future, this table might be a 3rd party authorization tool.
    table_constraint:
    fields:
      user_id:
        name: User ID
        description: User ID (a unique ID).
        type: text
        constraint: PRIMARY KEY
      user_name:
        name: User Name
        description: The user's full name.
        type: text
        constraint: NOT NULL
      user_email:
        name: User Email
        description: The user's email address.
        type: text
        constraint: UNIQUE NOT NULL
      user_affiliation:
        name: User Affiliation
        description: The user's primary affiliation, e.g. ECCC, BAM, etc.
        type: text
        constraint: NOT NULL
      admin:
        name: Admin User Flag
        description: Boolean flag for admin users.
        type: boolean
        constraint: NOT NULL

  species:
    name: Species
    description: >
      A table that lists all potential species, in our case, birds in Canada. 
      This table is saved to the database once, changes to the `species` 
      table require **admin** access directly in the database.
    table_constraint:
    fields:
      species_id:
        name: Species ID
        description: Upper case 4-letter code, e.g. `OVEN` for Ovenbird.
        type: text
        constraint: PRIMARY KEY
      scientific_name:
        name: Scientific Name
        description: Scientific name.
        type: text
        constraint: UNIQUE NOT NULL
      english_name:
        name: English Name
        description: Common name in English.
        type: text
        constraint: UNIQUE NOT NULL
      french_name:
        name: French Name
        description: Common name in French (use UTF-8 encoding).
        type: text
        constraint: UNIQUE NOT NULL

  components:
    name: Components
    description: >
      Components are SDM results that can be uploaded and evaluated. 
      Each component will have its Shiny module implementing the expected 
      behavior for upload, display, and evaluation. e.g. a component can have 
      corresponding `mod_<component_id>_ui` and `mod_<component_id>_server` 
      module functions. Mandatory components are prerequisites for starting 
      evaluations. Updates are done via parsing the `components` section of the
      `config.yaml` file by **admins** and **tool developers**. 
      After initial development, versioning and backward compatibility should 
      be a concern so that previous entries will not break.
    table_constraint:
    fields:
      component_id:
        name: Component ID
        description: Component ID, lower case with `_`, also used in Shiny module names.
        type: text
        constraint: PRIMARY KEY
      component_description:
        name: Component Description
        description: Component description.
        type: text
        constraint: NOT NULL
      component_mandatory:
        name: Mandatory
        description: Is the component mandatory (value is `TRUE`).
        type: boolean
        constraint: NOT NULL

  models:
    name: Models
    description: >
      A table listing models. The model name and ID acts as an abbreviation of 
      the model approach/extent/etc. and also acts as a version identifier, 
      e.g. `can_glm_v1.1` would mean that GLM models were used for models with 
      Canadian extent and this is version 1.1. Updates are done 
      by **modelers** via uploading or creating a method metadata file.
    table_constraint:
    fields:
      model_id:
        name: Model ID
        description: Model ID, lower case, alphanumeric, `.` and `_` allowed.
        type: text
        constraint: PRIMARY KEY
      model_name:
        name: Model Name
        description: A human readable version of `model_id` displayed in the app, can include spaces.
        type: text
        constraint: UNIQUE NOT NULL
      model_description:
        name: Model Description
        description: A human readable version of `model_id` displayed in the app.
        type: text
        constraint: NOT NULL
      # model_metadata:
      #   name: Model Metadata
      #   description: A file path or URL used to find the ODMAP metadata for the model.
      #   type: text
      #   constraint: NOT NULL

  materials:
    name: Materials
    description: >
      Upload materials are used to be displayed and evaluated. 
      Updates to the file system and the database will be regular as 
      **modelers** upload new materials.
    table_constraint:
    fields:
      material_id:
        name: Material ID
        description: ID for uploaded model materials (in the form of `<model_id>_<species_id>_<component_id>`), for model materials that apply to multiple species use `ALL` in place of the `species_id`.
        type: text
        constraint: PRIMARY KEY
      model_id:
        name: Model ID
        description: Model ID (foreign key), can be missing.
        type: text
        constraint: REFERENCES models (model_id)
      species_id:
        name: Species ID
        description: Species ID (foreign key).
        type: text
        constraint: REFERENCES species (species_id)
      component_id:
        name: Component ID
        description: Component ID (foreign key).
        type: text
        constraint: REFERENCES components (component_id)
      material_create_user:
        name: Create User
        description: User who uploaded the model material (foreign key).
        type: text
        constraint: REFERENCES users (user_id)
      material_create_time:
        name: Create Time
        description: Time of initial upload.
        type: timestamp
        constraint: NOT NULL
      material_modify_user:
        name: Modify User
        description: User who modified the model material (foreign key).
        type: text
        constraint: REFERENCES users (user_id)
      material_modify_time:
        name: Modify Time
        description: Time of last modification.
        type: timestamp
        constraint:
      material_settings:
        name: Material Settings
        description: Material settings, see `templates`.
        type: jsonb
        constraint: NOT NULL

  deployments:
    name: Deployments
    description: >
      Deployment is a set of results organized for a specific audience for
      evaluation. Deployments make materials reusable without having to
      repeatedly upload the same set of files. A deployment is defined
      by the **modeler** based on model materials that they are the
      owners (creators) of.
    table_constraint:
    fields:
      deployment_id:
        name: Deployment ID
        description: Deployment ID.
        type: text
        constraint: PRIMARY KEY
      deployment_name:
        name: Deployment Name
        description: Deployment name.
        type: text
        constraint: NOT NULL
      deployment_description:
        name: Deployment Description
        description: Deployment name.
        type: text
        constraint: NOT NULL
      deployment_create_user:
        name: Create User
        description: User who created the deployment (foreign key).
        type: text
        constraint: REFERENCES users (user_id)
      deployment_create_time:
        name: Create Time
        description: Time of deployment creation.
        type: timestamp
        constraint: NOT NULL
      deployment_settings:
        name: Deployment Settings
        description: Deployment settings, see `templates`.
        type: jsonb
        constraint: NOT NULL

  deployment_materials:
    name: Deployment Materials
    description: >
      Materials selected for a deployment. Entries in this table form the
      basis for evaluations and are defined by the **modeler**.
    table_constraint:
    fields:
      deployment_material_id:
        name: Deployment Material ID
        description: Compound ID for joining feedback to deployment materials (in the form of `<deployment_id>_<model_id>_<species_id>_<component_id>`).
        type: text
        constraint: PRIMARY KEY
      deployment_id:
        name: Deployment ID
        description: Deployment ID (foreign key).
        type: text
        constraint: REFERENCES deployments (deployment_id)
      material_id:
        name: Material ID
        description: Material ID.
        type: text
        constraint: REFERENCES materials (material_id)

  evaluations:
    name: Evaluations
    description: >
      Evaluations are the basic feedback on the materials within the context of
      a deployment. There is no explicitly defined primary key, sort and filter 
      to find entries to display. The body has to conform with component display 
      rules which is not checked by the database and is the job for the UI to 
      enforce. Updates to evaluations will be frequent as 
      **evaluators** provide more data feedback. Notes can be provided for
      evaluations, these function like flags marking feedback that needs
      to be addressed later (i.e. such notes can be identified later when
      synthesizing the evaluations).
    table_constraint: PRIMARY KEY (deployment_material_id, evaluation_create_user)
    fields:
      deployment_material_id:
        name: Deployment Material ID
        description: Deployment Material ID (foreign key).
        type: text
        constraint: REFERENCES deployment_materials (deployment_material_id)
      deployment_id:
        name: Deployment ID
        description: Deployment ID (foreign key).
        type: text
        constraint: REFERENCES deployments (deployment_id)
      material_id:
        name: Material ID
        description: Material ID (foreign key).
        type: text
        constraint: REFERENCES materials (material_id)
      component_id:
        name: Component ID
        description: Component ID (foreign key).
        type: text
        constraint: REFERENCES components (component_id)
      use_case:
        name: Use Case
        description: One use case applicable to the evaluation defined by the **modeler** for the deployment.
        type: text
        constraint: NOT NULL
      evaluation_create_user:
        name: Create User
        description: User who created the initial evaluation (foreign key).
        type: text
        constraint: REFERENCES users (user_id)
      evaluation_create_time:
        name: Create Time
        description: Time of initial the initial evaluation.
        type: timestamp
        constraint: NOT NULL
      evaluation_modify_user:
        name: Modify User
        description: User who last modified (foreign key).
        type: text
        constraint: REFERENCES users (user_id)
      evaluation_modify_time:
        name: Modify Time
        description: Time of last modification.
        type: timestamp
        constraint:
      evaluation_body:
        name: Evaluation
        description: JSON blob with the evaluation according to component display rules reflecting last modification.
        type: jsonb
        constraint: NOT NULL
      note_create_user:
        name: Note Create User
        description: User who created the note.
        type: text
        constraint: REFERENCES users (user_id)
      note_create_time:
        name: Note Create Time
        description: Time of note creation.
        type: timestamp
        constraint:
      note_body:
        name: Note Body
        description: JSON blob with the note on an evaluation reflecting last modification.
        type: jsonb
        constraint:

  comments:
    name: Comments
    description: >
      Comments are chat-like exchanges of ideas tied to a model-species 
      combination, visible to users with discussion roles.
      Display rule for comments are simpler than for evaluations, 
      we consider text comments. Users with **commenter**
      privileges can leave comments (like discussions back and forth) if
      comments are enabled for the deployment.
    table_constraint: PRIMARY KEY (deployment_id, model_id, species_id, comment_create_user)
    fields:
      deployment_id:
        name: Deployment ID
        description: Deployment ID (foreign key).
        type: text
        constraint: REFERENCES deployments (deployment_id)
      model_id:
        name: Model ID
        description: Model ID (foreign key).
        type: text
        constraint: REFERENCES models (model_id)
      species_id:
        name: Species ID
        description: Species ID (foreign key).
        type: text
        constraint: REFERENCES species (species_id)
      comment_create_user:
        name: Comment Create User
        description: User who created the note.
        type: text
        constraint: REFERENCES users (user_id)
      comment_create_time:
        name: Comment Create Time
        description: Time of comment creation.
        type: timestamp
        constraint: NOT NULL
      comment_body:
        name: Comment Body
        description: JSON blob with the note on an evaluation reflecting last modification.
        type: jsonb
        constraint: NOT NULL

  access:
    name: User Access
    description: >
      Describe what access users have for deployments, i.e. who the **modeler**
      wants to be an evaluator. This is a table that will be required for
      later phases of the tool development. We might have to consider
      access based on user groups as well to avoid having to set permissions
      for individuals, using existing groups instead.
    table_constraint: PRIMARY KEY (user_id, deployment_id)
    fields:
      user_id:
        name: User ID
        description: User ID (foreign key).
        type: text
        constraint: REFERENCES users (user_id)
      deployment_id:
        name: Deployment ID
        description: Deployment ID (foreign key).
        type: text
        constraint: REFERENCES deployments (deployment_id)
      user_roles:
        name: User Role
        description: A comma separated list of user roles, e.g. `modeler,evaluator`. The default role is `viewer` and is assumed even if not set by the user granting the access.
        type: text
        constraint: NOT NULL

components:
# note: properties used in the ui will have to take `en` and `fr` values.

  app:
    description: A special component to store high level reason for not completing an evaluation.
    mandatory: Yes
    type: "material"
    upload:
      type:
      input:
        description:
        file_ext:
        columns:
      output:
        description:
        columns:
        path: ""
    display:
      type:
      title:
      description:
      placement: "page.tab.section" # TBD
      material_settings:
    evaluation:
      evaluation_allowed: Yes

  observations:
    description: Detections or counts by location and timestamp for a single or multiple species.
    mandatory: Yes
    type: "material"
    upload:
      type: table
      input:
        description: >
          Observations are organized as survey events as rows (unique combinations
          of location [longitude/latitude] and date/time). The table has the
          following mandatory fields: latitude, longitude, time, and method.
          Latitude and longitude are coordinates as decimal degrees (EPSG:4326).
          Time is in a format coercible to POSIX time, e.g. 2019-06-01 7:21:00 AM.
          Method is a string denoting the survey method. Other columns holding
          the observations should be named using Species IDs, i.e. 4-letter codes
          (e.g. OVEN, TEWA, etc.) consistent with the species list being used.
          The cell values in the species columns should be non-negative integers
          (i.e. 0, 1, 2, ... or 0/1) indicating the detections (>0) and 
          non-detections of the species during the survey events.
          The single-species and multi-species data format are identical except
          for the number of species whose data is in the table.
          The table can be in csv (make sure that time is formatted properly),
          rds and parquet (these latter formats should have time as POSIX).
        file_ext: [csv, rds, parquet]
        columns: [latitude:double, longitude:double, time:timestamp, method:text]
      output:
        description: >
          The output is a spatial point data frame with columns:
          time, method, and status (coordinates are part of the geometry column).
        columns: [time:timestamp, method:text, status:integer]
        path: "materials/{model_id}/species/{species_id}/observations.gpkg"
    display:
      type: map
      title: Observations
      description: >
        Display non-detections (status == 0) detections (status > 0) in
        different colors. Show detections by default (green), allow an option to turn
        on non-detections (grey). Use circle markers in Leaflet.
        We can use unique(method) and year of survey as dropdown filters.
        Method, time, status should be part of popup message on click.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: "Observations."
          fr: ""
        units:
          en: "Counts"
          fr: ""
    evaluation:
      evaluation_allowed: Yes

  model_metadata:
    description: ODMAP protocol metadata.
    mandatory: Yes
    type: "material"
    upload:
      type: table
      input:
        description: TBD once we integrate ODMAP into the UI.
        file_ext: [csv, rds, parquet]
        columns: []
      output:
        description: TBD once we integrate ODMAP into the UI.
        columns: []
        path: "materials/{model_id}/metadata/model_metadata.parquet"
    display:
      type: table
      title: Model Metadata
      description: TBD once we integrate ODMAP into the UI.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: "ODMAP metadata for the model."
          fr: ""
    evaluation:
      evaluation_allowed: Yes

  predictor_metadata:
    description: Predictor metadata.
    mandatory: Yes
    type: "material"
    upload:
      type: table
      input:
        description: >
          The file should contain the recommended columns but is not
          strictly enforced. The table should give information about the
          predictors (one predictor per row). The file can be csv, rds, or parquet.
          The predictor column should list the short names of the predictors
          that match the band names in the predictor raster.
        file_ext: [csv, rds, parquet]
        columns: [predictor, definition, class, resolution, source, provider, citation, covariate_extraction, prediction_resolution, years, temporal_matching]
      output:
        description: A file is written with the listed columns.
        columns: [predictor, definition, class, resolution, source, provider, citation, covariate_extraction, prediction_resolution, years, temporal_matching]
        path: "materials/{model_id}/predictors/predictor_metadata.parquet"
    display:
      type: table
      title: Predictor Metadata
      description: >
        The table is displayed as a reactable table.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: "Predictor metadata table."
          fr: ""
    evaluation:
      evaluation_allowed: Yes

  predictor_raster:
    description: Predictor raster, a multi-band TIF with spatial predictors as bands.
    mandatory: No
    type: "material"
    upload:
      type: raster
      input:
        description: >
          A spatial raster file in tif format where bands are different spatial
          predictors. Names of the layers should match the predictor column in 
          the predictor metadata.
        file_ext: [tif]
        columns: []
      output:
        description: >
          The tif file is saved as is.
        path: "materials/{model_id}/predictors/predictor_raster.tif"
    display:
      type: map
      title: Predictor Raster
      description: >
        A raster map in Leaflet, bands are added as layers and can be selected
        from the layers icon of the map.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: "Predictor rasters."
          fr: ""
    evaluation:
      evaluation_allowed: Yes

  spatial_prediction:
    description: Expected value as band 1, variation as band 2.
    mandatory: Yes
    type: "material"
    upload:
      type: raster
      input:
        description: >
          A spatial raster in tif format. The 1st band is interpreted as
          the distribution layer (abundance, occurrence probability, density)
          with non-negative real values.
          The second layer, if present, is interpreted as a layer with
          uncertainty (non-negative values, SE, CoV, CI or PI width, etc.).
        file_ext: [tif]
        columns: []
      output:
        description: >
          The file is saved as is. The metadata will be used to
          label the map appropriately (what the units are etc.).
        path: "materials/{model_id}/species/{species_id}/spatial_prediction.tif"
    display:
      type: map
      title: Spatial Prediction
      description: >
        A raster map in Leaflet, with 2 bands: one for "abundance" one for
        "uncertainty". There is a single-species mode, and a comparison
        model. In comparison mode, we use a slider to reveal differences.
        We need a dropdown to choose which map to compare against.
        Same species & different model, same model & different species.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: "Spatial predictions."
          fr: ""
        units:
          en: "Density [inds/ha]"
          fr: ""
    evaluation:
      evaluation_allowed: Yes

  model_summary:
    description: Model summary including variable importance metrics or coefficients for predictor variables.
    mandatory: Yes
    type: "material"
    upload:
      type: table
      input:
        description: >
          A table summarizing the models, usually predictors as rows
          (listed in the column called predictor). The multi-species
          version lists Species IDs in the column species_id.
          The predictors should match names in the predictor metadata.
          Other columns can be named freely, e.g. var_importance, coefficient,
          StdError etc.
        file_ext: [csv, rds, parquet]
        columns: [species_id, predictor]
      output:
        description: >
          The input file is filtered for individual species and saved.
        path: "materials/{model_id}/species/{species_id}/model_summary.parquet"
    display:
      type: table
      title: Model Summary
      description: >
        The table is displayed as a reactable table.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: "Model summary."
          fr: ""
    evaluation:
      evaluation_allowed: Yes

  model_fit:
    description: Model fit statistics.
    mandatory: Yes
    type: "material"
    upload:
      type: table
      input:
        description: >
          A table summarizing the model fit, statistics as rows
          (listed in the column called statistic). The multi-species
          version lists Species IDs in the column species_id.
          The value of the fit statistic is in the column called value.
          Fit metrics are also listed in the project metadata.
        file_ext: [csv, rds, parquet]
        columns: [species_id, statistic, value]
      output:
        description: >
          The input file is filtered for individual species and saved.
        path: "materials/{model_id}/species/{species_id}/model_fit.parquet"
    display:
      type: table
      title: Model Fit
      description: >
        The table is displayed as a reactable table.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: "Model goodness-of-fit metrics."
          fr: ""
    evaluation:
      evaluation_allowed: Yes

  deployment_settings:
    description: Deployment settings.
    mandatory: Yes
    type: "deployment"
    upload:
      type: list
      input:
        description: >
          A JSON file specifying the settings according to the template
          `templates.deployment_settings`.
        file_ext: [json]
        columns: []
      output:
        description: >
          Settings saved into a JSON file.
        path: "deployments/{deployment_id}/deployment_settings.json"
    display:
      type: settings
      title: Settings
      description: >
        These settings define some aspects of the deployments.
        We can attach general evaluations to this component.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: ""
          fr: ""
    evaluation:
      evaluation_allowed: Yes

  deployment_subunits:
    description: Deployment subunits.
    mandatory: No
    type: "deployment"
    upload:
      type: polygons
      input:
        description: >
          A spatial polygons file with geometries and a key called 
          `subunit_id`. Each deployment can have its own subunits.
          Subunits can be used to make evaluations more specific.
        file_ext: [gpkg]
        columns: [subunit_id, geometry]
      output:
        description: >
          The input file is saved.
        path: "deployments/{deployment_id}/deployment_subunits.gpkg"
    display:
      type: polygons
      title: Deployment Subunits
      description: >
        Subunits are overlaid in top of raster maps.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: ""
          fr: ""
    evaluation:
      evaluation_allowed: No

  deployment_questions:
    description: Deployment questions.
    mandatory: No
    type: "deployment"
    upload:
      type: table
      input:
        description: >
          A csv file specifying the questions according to a default version.
        file_ext: [csv]
        columns: []
      output:
        description: >
          The input file is saved.
        path: "deployments/{deployment_id}/deployment_questions.csv"
    display:
      type: evaluations
      title: Questions
      description: >
        Questions will be displayed according to material type component
        specifications.
      placement: "page.tab.section" # TBD
      material_settings:
        legend:
          en: ""
          fr: ""
    evaluation:
      evaluation_allowed: No

default_questions: # where values is missing, we use the values from question_types
# --------- app --------
- component: app
  type: yes_no
  order: 1
  part: 0
  body:
    en: Are you sure that you want to quit the evaluation? If you quit, you can still continue the evaluation later.
    fr: ""
  followup_level: 0
  odmap_id: ~
- component: app
  type: simple_text
  order: 1
  part: 1
  body:
    en: Describe the reason why you do not wish to continue the evaluation.
    fr: ""
  followup_level: 0
  odmap_id: ~
# --------- spatial prediction --------
- component: spatial_prediction
  type: spatial
  order: 1
  part: 0
  body:
    en: Identify the areas on the map where predictions are inaccurate.
    fr: ""
  values: ["Severe over", "Moderate over", "Accurate", "Moderate under", "Severe under", "Unknown"]
  followup_level: 0
  odmap_id: ~
- component: spatial_prediction
  type: spatial
  order: 2
  part: 0
  body:
    en: Identify areas on the map where uncertainty does not align with your understanding of data limitations, known habitat complexities, or other sources of uncertainty.
    fr: ""
  values: ["Too high", "Accurate", "Unknown"]
  followup_level: 0
  odmap_id: ~
## ------------ model fit ------------
- component: model_fit
  type: ordinal
  order: 1
  part: 0
  body:
    en: How concerned are you about the adequacy or appropriateness of the validation metrics?
    fr: ""
  followup_level: 0
  odmap_id: [m_estim_1, m_estim_2, m_estim_3]
- component: model_fit
  type: simple_text
  order: 2
  part: 0
  body:
    en: What are the metrics that should be used and why?
    fr: ""
  followup_level: 0
  odmap_id: ~
- component: model_fit
  type: simple_text
  order: 3
  part: 0
  body:
    en: What other concerns do you have about model performance?
    fr: ""
  followup_level: 0
  odmap_id: [m_settings_1, m_settings_2, m_estim_1, m_estim_2, m_estim_3, m_selection_1, m_selection_2, m_selection_3, m_selection_4]
# ---------- observations ------------
- component: observations
  type: spatial
  order: 1
  part: 0
  body:
    en: Are there areas where you are concerned about potential inaccuracies in the data? Identify areas with potential inaccuracies in the data.
    fr: ""
  values: ["Very inaccuracies", "Moderately inaccuracies", "Accurate", "Unknown"]
  followup_level: 0
  odmap_id: ~
- component: observations
  type: spatial
  order: 2
  part: 0
  body:
    en: Are there areas where this species is not sufficiently sampled? Identify areas that are not sufficiently sampled.
    fr: ""
  values: ["Very undersampled", "Moderately undersampled", "Accurate", "Unknown"]
  followup_level: 0
  odmap_id: ~
- component: observations
  type: ordinal
  order: 3
  part: 0
  body:
    en: How concerned are you about the distribution of counts?
    fr: ""
  followup_level: 0
  odmap_id: ~
- component: observations
  type: simple_text
  order: 3
  part: 1
  body:
    en: If so, explain. What is an appropriate truncation value (i.e. highest count)?
    fr: ""
  followup_level: 0
  odmap_id: ~
- component: observations
  type: ordinal
  order: 4
  part: 0
  body:
    en: How concerned are you about the temporal distribution of detections?
    fr: ""
  followup_level: 0
  odmap_id: ~
- component: observations
  type: simple_text
  order: 4
  part: 1
  body:
    en: If so, explain. What is an appropriate truncation value (i.e. minimum year)?
    fr: ""
  followup_level: 0
  odmap_id: ~
- component: observations
  type: ordinal
  order: 5
  part: 0
  body:
    en: How concerned are you about the description and treatment of potential biases in the data, as described in the model metadata?
    fr: ""
  followup_level: 0
  odmap_id: [d_bio_10]
# ------------ model metadata -----------
- component: model_metadata
  type: ordinal
  order: 1
  part: 0
  body:
    en: Are you concerned about the description and treatment of predictor data, as described in the model metadata.
    fr: ""
  followup_level: 0
  odmap_id: [d_pred_2, m_multicol_1, m_preselect_1, m_estim_3]
# ------------ predictor metadata -----------
- component: predictor_metadata
  type: ordinal
  order: 1
  part: 0
  body:
    en: How concerned are you about missing predictors?
    fr: ""
  followup_level: 0
  odmap_id: [d_pred_2, m_multicol_1, m_estim_3]
- component: predictor_metadata
  type: simple_text
  order: 2
  part: 0
  body:
    en: What predictors are missing and why?
    fr: ""
  followup_level: 0
  odmap_id: [d_pred_2]
- component: predictor_metadata
  type: ordinal
  order: 3
  part: 0
  body:
    en: How concerned are you about superfluous or inappropriate predictors?
    fr: ""
  followup_level: 0
  odmap_id: [d_pred_2, m_estim_3]
- component: predictor_metadata
  type: simple_text
  order: 4
  part: 0
  body:
    en: Which predictors should be removed and why?
    fr: ""
  followup_level: 0
  odmap_id: ~
# ------------ predictor raster -----------
- component: predictor_raster
  type: ordinal
  order: 1
  part: 0
  body:
    en: How concerned are you about inaccuracy in the model predictors?
    fr: ""
  followup_level: 0
  odmap_id: [d_pred_2, p_uncertainty_1, p_uncertainty_2, p_uncertainty_3, p_uncertainty_4]
- component: predictor_raster
  type: simple_text
  order: 1
  part: 1
  body:
    en: If so, describe the predictor(s) and the areas of concern.
    fr: ""
  followup_level: 0
  odmap_id: [d_pred_2]
- component: predictor_raster
  type: ordinal
  order: 2
  part: 0
  body:
    en: How concerned are you about the spatial resolution of the predictor variables?
    fr: ""
  followup_level: 0
  odmap_id: [d_pred_2, m_preselect_1]
## ------------ model summary ------------
- component: model_summary
  type: ordinal
  order: 1
  part: 0
  body:
    en: How concerned are you about missing predictor interactions or nonlinear relationships?
    fr: ""
  followup_level: 0
  odmap_id: [d_pred_2, m_preselect_1]
- component: model_summary
  type: simple_text
  order: 2
  part: 0
  body:
    en: What interactions or nonlinear relationships are missing?
    fr: ""
  followup_level: 0
  odmap_id: [d_pred_2, m_preselect_1]
- component: model_summary
  type: ordinal
  order: 3
  part: 0
  body:
    en: How concerned are your about the reported relative importance of predictors?
    fr: ""
  followup_level: 0
  odmap_id: [m_estim_2]
- component: model_summary
  type: simple_text
  order: 4
  part: 0
  body:
    en: Which predictors would you expect to be more or less important and why?
    fr: ""
  followup_level: 0
  odmap_id: [m_estim_2]
- component: model_summary
  type: ordinal
  order: 5
  part: 0
  body:
    en: How concerned are you about the appropriateness of the modeling algorithm for the model objectives and application?
    fr: ""
  followup_level: 0
  odmap_id: [o_algorithms_1, o_algorithms_2]
- component: model_summary
  type: ordinal
  order: 6
  part: 0
  body:
    en: How concerned are you about how well the modelling framework addresses important statistical assumptions?
    fr: ""
  followup_level: 0
  odmap_id: [o_algorithms_2, o_workflow_1, o_objective_2, m_settings_1, m_settings_2, m_estim_1, m_estim_2, m_estim_3, m_selection_1, m_selection_2, o_workflow_2, m_selection_3, m_selection_4]
- component: model_summary
  type: ordinal
  order: 7
  part: 0
  body:
    en: How concerned are you about how well important biological assumptions are explained and justified?
    fr: ""
  followup_level: 0
  odmap_id: [o_objective_1, o_objective_2, o_concept_1, o_assumptions_1]
- component: model_summary
  type: simple_text
  order: 8
  part: 0
  body:
    en: What other concerns or comments do you have about model complexity?
    fr: ""
  followup_level: 0
  odmap_id: [m_settings_1, o_algorithms_2]
# ---------- deployment settings --------
- component: deployment_settings
  type: simple_text
  order: 1
  part: 0
  body:
    en: Are there any non-target applications that you think this model is unsuitable for? Please explain.
    fr: ""
  followup_level: 0
  odmap_id: [o_objective_1, o_objective_2, o_utility_2, o_utility_3]
- component: deployment_settings
  type: ordinal
  order: 2
  part: 0
  body:
    en: How concerned are you about how well suited the model is for the intended application of the model?
    fr: ""
  followup_level: 0
  odmap_id: [o_objective_1, o_utility_2]
- component: deployment_settings
  type: simple_text
  order: 3
  part: 0
  body:
    en: What other concerns or comments do you have about using the model for the intended model applications?
    fr: ""
  followup_level: 0
  odmap_id: [o_objective_1, o_utility_2]
# --------- deployment_subunits ------------
- component: deployment_subunits
  type: simple_text
  order: 1
  part: 0
  body:
    en: Describe your overall assessment regarding this species.
    fr: ""
  followup_level: 0
  odmap_id: ~
- component: deployment_subunits
  type: spatial
  order: 2
  part: 0
  body:
    en: Identify the areas on the map where you have hight confidence about your knowledge of the species.
    fr: ""
  values: ["Confident"]
  followup_level: 0
  odmap_id: ~

question_types:
  simple_text:
    name: Simple Text Input
    description: Free form text input.
    values: [""]
  yes_no:
    name: Yes/No Question
    description: A Yes or No question. Follow up questions are conditional on Yes.
    values: ["Yes", "No"]
  # gold_standard:
  #   name: Gold Standard
  #   description: A 1-5 scale (Unk - Gold) ordinal response presented as a dropdown.
  #   values: ["Gold", "Silver", "Bronze", "Deficient", "Unknown"]
  ordinal:
    name: Ordinal
    description: "Ordinal for importance or difficulty (-2 -- +2) to answer questions such as: To what degree ..."
    values: ["Extremely", "Very", "Moderately", "Slightly", "Not at all", "Uncertain"]
  spatial:
    name: Spatial
    description: A list of subunit IDs saved for each of the values.
    values: []
# severe overprediction, moderate overprediction, accurate, moderate underprediction, severe underprediction, unknown

followup_questions: # used for drill-down
- followup_level: 1
  type: simple_text
  body:
    en: Why is it a problem?
    fr: ""
- followup_level: 2
  type: ordinal
  body:
    en: How important is it to fix the problem?
    fr: ""
  values: ["Very important", "Moderately important", "Not important"]
- followup_level: 3
  type: ordinal
  body:
    en: How difficult is it to fix the problem?
    fr: ""
  values: ["Very difficult", "Moderately difficult", "Easy"]
- followup_level: 4
  type: simple_text
  body:
    en: Please suggest options for addressing the problem. To what degree would the suggested fixes address the problem?
    fr: ""
- followup_level: 5
  type: ordinal
  body:
    en: If the problem is not fixed, can this model be used for the intended application?
    fr: ""
  values: ["Yes", "No"]
- followup_level: 6
  type: simple_text
  body:
    en: If the problem is not fixed, what caveats or guidance should be provided to users?
    fr: ""


templates:

  deployment_settings:
    instructions_to_evaluators:
      en: "Provide instructions for the evaluation."
      fr: ""
    use_case:
      en: "Use case"
      fr: "Cas d'utilisation"
    comments_allowed: No
    preferred_language: "en"
