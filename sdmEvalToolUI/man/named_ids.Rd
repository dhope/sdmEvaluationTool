% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{named_ids}
\alias{named_ids}
\title{Create named lists of ids for input selections}
\usage{
named_ids(df_db, id = "id", name = "name", match = NULL)
}
\arguments{
\item{df_db}{Data frame or database where ID values can be found.}

\item{id}{Character. Pattern matching 'id' in the column name.}

\item{name}{Character. Pattern matching 'name' in the column name.}

\item{match}{Character. ID/Name preface. Optional instead of \code{id}/\code{name},
useful if there are more than one pair of id/name in the data frame.}
}
\value{
Named character vector.
}
\description{
Lists are c(named = id) to create a input selection with 'pretty' selections
returning original ids.
}
\examples{
\dontshow{if (have_data()) withAutoprint(\{ # examplesIf}
df <- data.frame(
  species_id = c("X11", "Y22"),
  species_name = c("Pretty name", "Lovely name")
)

named_ids(df)
named_ids(df, match = "species")
\dontshow{\}) # examplesIf}
}
