% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{add_raster}
\alias{add_raster}
\title{Add raster as image layer to leaflet map}
\usage{
add_raster(
  map,
  raster,
  layer,
  name,
  palette,
  opacity = 0.8,
  add_legend = TRUE,
  min_0 = TRUE
)
}
\arguments{
\item{map}{Leaflet map}

\item{raster}{Terra raster to add}

\item{layer}{Layer in raster to add}

\item{name}{Name of leaflet layer (for controls and ids)}

\item{palette}{Character. Colour palette to use}

\item{opacity}{Numeric. Opacity of raster layer}

\item{add_legend}{Logical. Whether or not to add a legend.}

\item{min_0}{Logical. Whether to force the legend to start at zero.}
}
\value{
Leaflet map
}
\description{
Note that raster layers are attached to a new map pane with a defined
z-index. This is so we can be sure the raster is above the map tiles (z-index
200, see \code{base_map()}), and yet below the subunits (default z-index of 400).
}
\examples{
r <- template_spatial_prep()
base_map() |>
  add_raster(r, layer = "mean", palette = "viridis", name = "Mean")
}
