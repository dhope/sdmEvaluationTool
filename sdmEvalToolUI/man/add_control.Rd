% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{add_control}
\alias{add_control}
\title{Add Layers control to map}
\usage{
add_control(map, groups = character(0))
}
\arguments{
\item{map}{Leaflet map}

\item{groups}{Character vector. Layers or Group names to add to the control.
Subunits are included by default. Only existing layers are included.}
}
\value{
Leaflet map with layer control buttons
}
\description{
Add Layers control to map
}
\examples{
# No subunits
r <- template_spatial_prep()
base_map() |>
  add_raster(r, layer = "mean", palette = "viridis", name = "Mean") |>
  add_control(groups = "Mean")

# With subunits
if(have_data()) {
  r <- template_spatial_prep()
  s <- deployment_subunits_prep("deployment1")
  base_map() |>
    add_raster(r, layer = "mean", palette = "viridis", name = "Mean") |>
    add_subunits(s) |>
    add_control(groups = "Mean")
}
}
